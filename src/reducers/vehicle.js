export default (vehicleId=null, action={}) => {
    // const { type, payload } = action

    // switch (type) {
    //     case INCREMENT: return counterState + 1
    // }

    // return counterState
    return vehicleId;
}